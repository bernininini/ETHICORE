
  # Doctor Bias Detector Dashboard

  This is a code bundle for Doctor Bias Detector Dashboard. The original project is available at https://www.figma.com/design/UcwU9GAZAvmJR7ZOBKTIAB/Doctor-Bias-Detector-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  